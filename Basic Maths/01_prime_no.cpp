#include<iostream>
using namespace std;

bool isPrime(int n)
{

}

int main()
{
    int n;
    cin>>n;

    if(isPrime(n))
        cout<<"it is prime no."<<endl;
    else  
        cout<<"not a prime no."<<endl;
        
    return 0;
}